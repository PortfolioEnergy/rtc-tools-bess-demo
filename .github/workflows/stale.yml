name: Close stale PRs

on:
  schedule:
    - cron: "27 4 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: PortfolioEnergy/shared-github-actions/.github/workflows/stale.yml@v2.1.1
